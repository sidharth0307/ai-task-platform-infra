apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-redis
  namespace: ai-task-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-redis
  template:
    metadata:
      labels:
        app: ai-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: ai-redis
  namespace: ai-task-platform
spec:
  selector:
    app: ai-redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379